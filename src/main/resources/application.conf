ktor {
  deployment {
    port = 3000
    port = ${?HTTP_PORT}
  }
  application {
    modules = [ eu.miaplatform.service.ServiceApplicationKt.module ]
  }
  log {
    level = ${?LOG_LEVEL}
    httpLogLevel = ${?HTTP_LOG_LEVEL}
  }
}